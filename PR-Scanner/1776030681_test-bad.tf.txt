provider "aws" {
  region = "us-east-1"
}

resource "aws_s3_bucket" "bad_bucket" {
  bucket = "my-public-bucket-rahul"

  acl = "public-read-write"   # ❌ public access

  versioning {
    enabled = false   # ❌ versioning disabled
  }
}

resource "aws_security_group" "bad_sg" {
  name        = "allow_all"
  description = "Allow all traffic"

  ingress {
    from_port   = 0
    to_port     = 0
    protocol    = "-1"
    cidr_blocks = ["0.0.0.0/0"]   # ❌ open to world
  }
}

resource "null_resource" "bad_script" {
  provisioner "local-exec" {
    command = "echo password=admin123 >> secrets.txt"   # ❌ hardcoded secret
  }
}